import express from 'express'
import { description, name, version } from '../../package.json'
const routes = express.Router()

routes.get(`/`, (req, res) => res.json({ name, description, version }))

export default routes